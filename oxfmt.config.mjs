// @ts-check
/** @type {import('oxfmt').Config} */
export default {
  // Match common defaults; keep diffs small and consistent across the monorepo.
  semicolons: false,
  singleQuote: true,
  printWidth: 100,
};
